<div class="network-metrics left">
  <div class="chart-container"></div>
</div>

<div class="wrapper">
  <div class="network-filters-container">
    <ul class="network-filters">
      <li class="tab-menu first <%= params.has_key?(:member_type) ? '' : 'active' %>">
        <%= link_to "All", params.merge(:member_type => nil, :page => nil), :class => "label" %>
      </li>
      <li class="tab-menu <%= params[:member_type] == 'Professional' ? 'active' : '' %>">
        <%= link_to "Professionals", params.merge(member_type: "Professional", :page => nil), :class => "label" %>
      </li>
      <li class="tab-menu last <%= params[:member_type] == 'Student' ? 'active' : '' %>">
        <%= link_to "Students", params.merge(member_type: "Student", :page => nil), :class => "label" %>
      </li>
      <li class="dropdown-menu">
        <a class="label">Country</a>
        <div class="filter">
          <a class="icon icon-arrow-down"></a>
          <ul>
            <li><%= link_to "Philippines", params.merge(country: "Philippines", :page => nil) %></li>
            <li><%= link_to "Japan", params.merge(country: "Japan", :page => nil) %></li>
          </ul>
        </div>
      </li>
      <li class="dropdown-menu">
        <a class="label">Major Field</a>
        <div class="filter">
          <a class="icon icon-arrow-down"></a>
          <ul>
            <li><a href="/network?major_field="></a></li>
            <% @major_fields.each do |f| %>
            <li><%= link_to f.major_field_name, params.merge(major_field: f.major_field_name, :page => nil) %></li>
            <% end %>
          </ul>
        </div>
      </li>
    </ul>
  </div>
  <div class="network-table-container">
    <table class="network-table">
      <thead>
        <tr>
          <th>Name</th>
          <th>Occupation</th>
          <th>Major</th>
          <th>Affiliation</th>
          <th>Field Keywords</th>
          <th>Contact</th>
        </tr>
      </thead>
      <tbody>
        <% @users.each_with_index do |u,i| %>
        <tr class="<%= i.odd? ? 'odd' : 'even' %>">
          <td><%= u.user_info.first_name %> <%= u.user_info.last_name %></td>
          <td><%= u.user_info.occupation %></td>
          <td><%= u.user_info.major_field_name %></td>
          <td><%= u.user_info.organization_one %></td>
          <td><%= u.user_info.field_keywords %></td>
          <td><%= u.email %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <%= will_paginate @users %>
</div>